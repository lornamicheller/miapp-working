import { ElementRef, Renderer, OnInit } from '@angular/core';
import { ModalController } from '@ionic/angular';
import { NgModel, NgControl } from '@angular/forms';
export declare class LiIonicTimepickerDirective implements OnInit {
    private modalCtrl;
    ngModel: NgModel;
    control: NgControl;
    el: ElementRef;
    renderer: Renderer;
    inputTimeConfig: any;
    isModalOpen: any;
    closeIcon: any;
    selectedTime: any;
    constructor(modalCtrl: ModalController, ngModel: NgModel, control: NgControl, el: ElementRef, renderer: Renderer);
    ngOnInit(): void;
    onFocus(): void;
    openTimePicker(): Promise<void>;
}
